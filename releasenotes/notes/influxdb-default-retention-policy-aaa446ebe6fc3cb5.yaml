---
features:
  - |
    Support default_retention_hours config for InfluxDB for new
    database that are created. Falls back to infinite retention as
    before when unspecified or if the value is 0. Only applicable when
    db_per_tenant is enabled. Retention policy for existing databases
    are not altered. Must be an integer.
