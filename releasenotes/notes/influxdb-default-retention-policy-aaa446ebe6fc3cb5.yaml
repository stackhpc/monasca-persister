---
features:
  - |
    Support default_retention_policy config for InfluxDB for new
    database that are created. Falls back to infinite retention as
    before when unspecified. Only applicable when db_per_tenant is
    enabled. Retention policy for existing databases are not altered.
    Must be specified in format such as 1w, 1m, 1y, etc.
